## Usage

```
$ npm install grunt-cli // only if you don't already have grunt installed
$ npm install
$ classPrefix="vjs-icon-" fontName="VCPlayer" font="player" grunt
$ classPrefix="icon-" fontName="Authen" font="authenticate" grunt

```